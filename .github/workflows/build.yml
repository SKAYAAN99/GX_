name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Extract Project Zip
        run: |
          unzip -o "airtel_gx_manager (2).zip" -d extracted_app

      - name: Locate Project Root
        id: find_dir
        run: |
          if [ -f "extracted_app/pubspec.yaml" ]; then
            echo "PROJECT_DIR=extracted_app" >> $GITHUB_ENV
          else
            INNER_DIR=$(find extracted_app -name "pubspec.yaml" -exec dirname {} \;)
            echo "PROJECT_DIR=$INNER_DIR" >> $GITHUB_ENV
          fi

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Generate Missing Android Platform Files
        run: |
          cd "$PROJECT_DIR"
          flutter create . --platforms=android --org=com.gicbroadband

      - name: Enable HTTP & Disable Security Checks
        run: |
          cd "$PROJECT_DIR"
          if [ -f "android/app/src/main/AndroidManifest.xml" ]; then
            sed -i 's/<application/<application android:usesCleartextTraffic="true"/g' android/app/src/main/AndroidManifest.xml
          fi
          
          sed -i '1s/^/import "dart:io";\nclass MyHttpOverrides extends HttpOverrides { @override HttpClient createHttpClient(SecurityContext? context){ return super.createHttpClient(context)..badCertificateCallback = (X509Certificate cert, String host, int port)=> true; } }\n/' lib/main.dart
          sed -i '/void main()/a \  HttpOverrides.global = MyHttpOverrides();' lib/main.dart

      - name: Get Dependencies
        run: |
          cd "$PROJECT_DIR"
          flutter pub get

      - name: Build Debug APK
        run: |
          cd "$PROJECT_DIR"
          flutter build apk --debug --no-tree-shake-icons

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GIC-Broadband-App
          path: "${{ env.PROJECT_DIR }}/build/app/outputs/flutter-apk/*.apk"
